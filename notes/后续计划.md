## 后续剩余计划：

  1. 完整自动化测试
     当前已有 TOTP/恢复码单元测试，但还需要认证、照片审核、配额并发、AI 会话隔离的接口测试和端到端测试。

  2. AI 会话管理
     当前已持久化会话和消息，但还缺：
      - 会话列表；
      - 会话详情；
      - 删除/归档；
      - 管理端或用户端会话管理界面。

  3. Jobs 运维面板
     当前内容流水线 Jobs 可运行和查询，但还需要：
      - 失败重试；
      - 取消任务；
      - 任务详情；
      - 更完整的统一 Jobs 状态面板。

  4. 存储运维增强
     当前统计接口已完成，后续可增加：
      - orphan 文件扫描；
      - 缩略图重建；
      - 存储一致性校验；
      - 清理确认和审计。

  5. 部署运维优化
      - 自动保留最近 N 个发布包；
      - 自动保留一个回滚镜像；
      - 设置 Docker/systemd 日志上限；
      - 将当前清理策略写入部署脚本，避免再次积满磁盘。