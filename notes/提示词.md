经过实际页面操作发现问题如下：1.严重问题，用户管理这里创建用户，角色分为访客、管理员、朋友，分组又有family\friends\close-friends，两者作用不清晰，覆盖范围存在问题，这里角色应该是对应后台操作权限，应该是管理员和普通用户，根本就不需要访客，
  访客为什么要有账号？对于分组，它的作用是什么？我觉得是能够快速将上传的内容比如图片、作品集、相册等的可见性或者可操作性分给对应用户，但是现在似乎并没有这个效果，因此我觉得分组是可以手动增加删除自定义的。2.基于分组的设置，照片、作品集、相册
  都应该有相应的属性，比如我把这个相册设置为朋友分组，则属于朋友的用户就可以查看这个相册，一个用户可以是多个分组，相册同理。请明确设计好分组的作用，并更新所有相应代码。3.关于用户权限是否实际同步，比如对于相册管理普通用户就不能管理相册，他只
  有查看功能，普通用户可以进行的操作应该就是上传和管理属于它的图片，并且这个属于它也需要区分，如果上传图片后管理员将其分类和可见性等改变了，此时该用户看不到该照片了，那此时还能管理吗？请设计这里的逻辑，用户和权限和上传数据的关系。4.现在经过
  实测登入111账户上传图片时可以选择私有，这里的意义是什么，私有照片理论上的逻辑是不是只有ubuntu服务器上能够查看还是说管理员都能查看？此外因为前面修改了，前端上传照片这里也应该修改，一个是上传时可以选择上传到哪个或哪几个相册或者直接就是上传
  照片不归到某个相册，另一个是选择其照片属于的分类，这两个都应该是当前用户可以访问的有权限的，比如其用户所属分类，其可看到的相册。并且这里上传如果是普通用户则仅仅是上传还需要后台审核，如果是管理员用户默认审核完成。或者另一种设计前台页面只有
  一个上传和写标题的功能，默认都需要到后台有管理员权限的再进行审核照片，或者将照片分类或照片归类相册的操作。请选择一种。5.我现在根本就没有看到审查功能，对于http://120.26.231.150/admin/photos，照片都没有审查过程，审核 / 同步这里显示的始终
  是“待处理 原图:待处理 缩略图：ready”。6.为什么普通用户没有权限的也能看到后台的很多功能，理论上普通用户只需要看到自己上传的照片和存储管理等即可，虽然现在普通用户点击时实际操作会报错，但是就不应该让其能访问这些页面。7.我说的相册看不到照片不
  是指前台页面，而是http://120.26.231.150/admin/albums这里。8.为什么现在还是无法实现回流，原图：始终都是待处理，点击重试失败回流仅仅显示任务已重新排队，Ubuntu 定时任务会自动处理，实际没有任何操作，以及这里定时是多久，是因为还没到定时的原因
  吗？我现在等待了一个小时左右。9.本地高权限操作我实际并没有见到，我现在在非本地登录管理员账号还是能点击同步skill，在ubuntu登录管理页面也没有显示任何特殊的提示或者额外的功能，我在ubuntu输入的地址是http://120.26.231.150/admin/hermes/skills，
  是需要输入本地地址吗？10.请优化前端[Image #1] ，如图所示。11.[Image #2] ，如图所示，根本没有获取到实际页面大小，导致信息都堆积到中间的一小部分，然后加上要求调整的最多20篇，导致现在基本信息都显示不全，既然是要求最多20篇，那就意味着需要保证
  每一个博客元素的最小尺寸，然后再根据页面去自动调整数量，而不是默认20个的数量直接缩小每一个的大小。