  已完成的是：基础架构、ECS/Ubuntu 联合部署、原图回流、缩略图同步、文章搜索、构建产物管理等；最终验收报告也已生成，但报告状态仍是 partial。部分高风险业务流程尚未用真实测试账号和测试照片验证，另外部分 Hermes、Skill、用户上传页面仍不完整。

  你可以按下面顺序在前端验收。

  阶段 1：基础规则与项目结构

  无直接前端页面。主要验收：

  - 登录后台后页面能正常加载。
  - 公开首页、相册、作品集、博客均可访问。
  - 后台路由不会在未登录时直接展示数据。

  页面：

  - /
  - /albums
  - /portfolio
  - /blog
  - /admin/login

  阶段 2：数据库与权限基础

  页面：

  - /admin/users
  - /admin/albums
  - /admin/photos

  请确认：

  - 用户列表能加载。
  - 用户角色、分组、AI 权限、上传配额可编辑。
  - 相册可设置 public、friends、private。
  - 照片状态和可见范围能够显示。

  阶段 3：登录、用户组和 AI 权限

  页面：

  - /admin/login
  - /admin/users
  - /ai

  请使用不同账号确认：

  - 未登录访问 /ai 会被拦截。
  - 无 AI 权限用户不能使用聊天。
  - 有 AI 权限用户可以进入 /ai。
  - 用户的角色、分组和 AI 等级在后台显示正确。

  阶段 4：照片审核流程

  页面：

  - /admin/photos
  - /admin/albums
  - /albums

  需要使用测试账号和测试照片确认：

  1. 用户上传照片后状态为“待处理”。
  2. 管理员批准后照片可按指定可见范围展示。
  3. 拒绝后照片不出现在公开页面。
  4. public、friends、private 的访问范围符合预期。
  5. 朋友账号不能访问私有照片。

  阶段 5：照片存储和服务器同步

  页面：

  - /admin/storage
  - /admin/photos
  - /albums/[id]

  请确认：

  - 存储页面能看到照片总数、估算空间、同步状态。
  - 照片状态能显示“待同步、已同步、失败”。
  - 公开照片可以在 ECS 页面正常显示。
  - 私有照片不会因为同步流程出现在公开相册。

  服务器链路已经完成基础健康检查，但实际照片回流仍建议用一张临时测试照片验证。

  阶段 6：后台管理界面

  页面：

  - /admin
  - /admin/photos
  - /admin/albums
  - /admin/albums/[id]
  - /admin/users
  - /admin/storage
  - /admin/audit

  请确认：

  - 后台各模块能正常打开。
  - 相册创建、编辑、删除权限正确。
  - 相册内照片添加和移除正常。
  - 照片状态、可见性修改后页面即时刷新。
  - 审计页面能够看到管理操作记录。

  阶段 7：AI Gateway 与 Hermes 安全聊天

  页面：

  - /ai
  - /admin/hermes
  - /admin/audit

  需要确认：

  - 授权用户可以正常聊天。
  - 普通 AI 用户不能执行 shell、删除文件或控制设备。
  - AI 请求会产生审计记录。
  - 未授权用户访问 /ai 返回拒绝。

  该阶段的安全边界尚未完成完整人工验收。

  阶段 8：内容流水线

  页面：

  - /admin/content-pipeline
  - /admin/articles
  - /admin/articles/new
  - /admin/articles/[id]
  - /blog
  - /blog/archive

  请确认：

  - 可以创建或导入 Markdown 内容。
  - AI/人工内容进入候选状态。
  - 候选内容可以编辑、退回、批准。
  - 未批准文章不会出现在公开博客。
  - 发布后文章出现在 /blog。
  - 文章搜索可以按关键词过滤。

  文章实际存储在挂载数据卷中，不应依赖 Git 跟踪。

  阶段 9：Skill Registry

  页面：

  - /skills
  - /admin/hermes/skills

  请确认：

  - Skill 列表可以查看。
  - Skill 详情可以打开。
  - 状态、标签和统计信息可以显示。
  - 普通远程用户不能执行 Skill 同步或修改操作。

  注意：部分 Skill 同步逻辑仍需单独确认，不能仅凭页面存在判断全部完成。

  阶段 10：本地高权限操作

  页面：

  - /admin/local-ops
  - /admin/audit

  请确认：

  - 普通管理员不能执行高危操作。
  - 删除、Skill 同步、命令执行等操作需要本地可信来源。
  - 高危操作有二次确认。
  - 操作结果和操作者写入审计日志。

  阶段 11：运维、备份、告警和日志

  页面：

  - /admin/storage
  - /admin/audit

  前端可查看：

  - 存储统计。
  - 同步状态。
  - 审计记录。

  服务器侧还需要额外确认：

  - 数据库备份可恢复。
  - 定时任务执行成功。
  - 日志和告警正常。
  - ECS 不直接承担长期原图存储。

  阶段 12：最终集成验收

  这不是单独的业务页面，而是综合验收：

  - 公开访客访问公开内容。
  - 朋友账号访问授权内容。
  - 管理员进行审核和用户管理。
  - AI 用户只能使用受限能力。
  - 私有照片不会公开。
  - 数据恢复和审计链路有效。

  当前该阶段状态是“部分通过”，不是全部通过。

  阶段 13：照片状态同步策略

  主要页面：

  - /admin/photos
  - /admin/storage
  - /albums/[id]

  请重点确认：

  - 照片从 pending 改为 approved/public 后进入同步流程。
  - 照片改为私有或拒绝后，不会继续出现在公开目录。
  - 同步失败时页面显示失败状态，而不是误报成功。

  阶段 14：用户上传与重新提交

  当前可确认的页面主要仍是：

  - /admin/photos
  - /admin/users

  但目前没有完整独立的“我的上传”前端页面可供完整验收。因此以下事项仍不能算完成：

  - 用户查看自己的上传列表。
  - 用户查看审核意见。
  - 被拒绝后重新提交。
  - 上传配额和状态提示。

  建议你先按阶段 2、3、4、6、8、9、10 的页面验收；阶段 1、5、11、12、13 还需要结合服务器状态和测试照片；阶段 14 需要先补齐用户侧页面后才能完整验收。